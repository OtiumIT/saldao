{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "deployCommand": "npx wrangler pages deploy dist --project-name=dvd --commit-dirty=true",
  "framework": "vite",
  "nodeVersion": "20"
}
