{
  "name": "gestao-financeira",
  "version": "1.0.0",
  "type": "module",
  "description": "Sistema de Gestão Financeira, Projetos e Custos",
  "scripts": {
    "setup": "node setup-local.js",
    "deploy:site": "cd api && npx wrangler pages deploy ../site --project-name=saldao",
    "deploy:api": "cd api && npm install && npx wrangler deploy"
  }
}
