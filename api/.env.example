# Autenticação fixa (por hora, sem Supabase)
FIXED_AUTH=true
FIXED_AUTH_EMAIL=admin@saldao.local
FIXED_AUTH_PASSWORD=senha123
JWT_SECRET=saldao-jwt-secret-change-in-production

# PostgreSQL – local ou Supabase (produção)
# Local: postgresql://localhost:5432/saldao_jerusalem
#
# Supabase – conexão direta (porta 5432):
#   postgresql://postgres:[YOUR-PASSWORD]@db.PROJECT_REF.supabase.co:5432/postgres
#   Ex.: postgresql://postgres:[YOUR-PASSWORD]@db.eoieosbjgwskiobsuplz.supabase.co:5432/postgres
#
# Supabase – POOLER (porta 6543, recomendado para Worker/Hyperdrive e serverless):
#   No dashboard: Project Settings → Database → Connection string → URI (Session pooler)
#   postgresql://postgres.[PROJECT-REF]:[SENHA]@aws-0-[REGIAO].pooler.supabase.com:6543/postgres
#   Senha com @ use %40 (ex.: senha@123 → senha%40123)
#
DATABASE_URL=postgresql://localhost:5432/saldao_jerusalem

# Supabase (obrigatório para usar Data API)
# Para usar Supabase Data API em vez de PostgreSQL direto, configure:
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# Migração de dados (script lê api/.env): DATABASE_URL = origem, SUPABASE_DATABASE_URL = destino
# SUPABASE_DATABASE_URL=postgresql://postgres.[PROJECT]:[SENHA]@aws-0-[REGIAO].pooler.supabase.com:6543/postgres
# USE_SUPABASE_DATA_API=true  # Opcional: força uso de Data API (padrão: true se Supabase configurado)
# 
# IMPORTANTE: Use SERVICE ROLE KEY (não anon key) - bypassa todas as policies (RLS)
# Encontre em: Dashboard → Settings → API → Project API keys → service_role

# OpenAI (igual ao raioxfinanceiro: scripts/generate e frontend usam OPENAI_API_KEY)
# Obrigatório para: extração por foto em Pedidos de compra e Vendas (Compras → enviar foto, Vendas → extrair da imagem).
# Crie em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sua-chave-openai

# Email Configuration (Gmail)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password-here
CONTACT_EMAIL=contact@example.com
SEND_CONFIRMATION_EMAIL=false

# Server Configuration
PORT=3000
CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173
